#!/bin/bash
# Auto-generated by Theme-Engine

# GTK System Fonts
gsettings set org.gnome.desktop.interface font-name '{{ .Theme.Theme.Fonts.System.Family }} {{ .Theme.Theme.Fonts.System.Size }}'
gsettings set org.gnome.desktop.interface document-font-name '{{ .Theme.Theme.Fonts.System.Family }} {{ .Theme.Theme.Fonts.System.Size }}'
gsettings set org.gnome.desktop.interface monospace-font-name '{{ .Theme.Theme.Fonts.Terminal.Family }} {{ .Theme.Theme.Fonts.Terminal.Size }}'

# Cursor Size (Fallback to 24 if not configured, though we don't have cursor size yet)
# gsettings set org.gnome.desktop.interface cursor-size 24

echo "System fonts applied via gsettings."
